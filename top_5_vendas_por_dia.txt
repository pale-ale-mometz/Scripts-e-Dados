SELECT
    DATE(DT_FILIACAO) AS sale_date,
    COUNT(DISTINCT CONCAT(cpf, '|', DATE(DT_FILIACAO))) AS total_sales
FROM mysql_bi_g.NOMINAL_VENDAS
WHERE DT_FILIACAO >= '2024-01-01'
GROUP BY DATE(DT_FILIACAO)
ORDER BY total_sales DESC
LIMIT 5;